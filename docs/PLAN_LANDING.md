### 🗺️ ПЛАН ВЫПОЛНЕНИЯ

**Задача:** Создание production-ready лендинга Intervo и публикация в GitHub

**Тип:** новая функциональность (финализация проекта + публикация)

**Стратегия:** Проект уже содержит разделённые `index.html` и `styles.css` с полной структурой лендинга, адаптивной вёрсткой и подключёнными шрифтами. Основная работа — аудит качества существующего кода, добавление служебных файлов проекта, инициализация git и публикация в GitHub.

---

### 📐 ЭТАПЫ

#### Этап 1: Аудит и доработка существующего кода

**Цель этапа:** Убедиться, что `index.html` и `styles.css` соответствуют production-уровню, исправить найденные проблемы.

| # | Подзадача | Файлы/компоненты | Зависит от | Checkpoint |
|---|-----------|-------------------|------------|------------|
| 1.1 | Проверить HTML на валидность: наличие `lang="en"` (лендинг на русском — оставить `lang="ru"`), корректность мета-тегов, семантику разметки | `index.html` | — | HTML проходит валидацию W3C без ошибок, семантические теги используются корректно |
| 1.2 | Проверить CSS: неиспользуемые стили, дублирование правил, корректность fallback-шрифтов | `styles.css` | — | Нет дублирующих или неиспользуемых правил |
| 1.3 | Проверить адаптивность на брейкпоинтах 1024px и 680px: все секции корректно перестраиваются, нет горизонтального скролла, текст читаем | `index.html`, `styles.css` | 1.1, 1.2 | Визуальная проверка в браузере на 3 размерах: desktop (1440), tablet (1024), mobile (375) |
| 1.4 | Убедиться, что навигационные ссылки (`#audience`, `#formats`, `#pricing`, `#cta`, `#demo`) указывают на существующие `id` в HTML | `index.html` | 1.1 | Все якорные ссылки работают, скролл к нужной секции |
| 1.5 | Проверить, что `mailto:hello@intervo.dev` в CTA-кнопке корректно оформлен | `index.html` | — | Клик по кнопке открывает почтовый клиент |

**Definition of done этапа:** HTML валиден, CSS чист, адаптивность работает на всех брейкпоинтах, все внутренние ссылки ведут к нужным секциям.

---

#### Этап 2: Добавление служебных файлов проекта

**Цель этапа:** Подготовить репозиторий к публикации — добавить `.gitignore`, `README.md` и опционально `favicon`.

| # | Подзадача | Файлы/компоненты | Зависит от | Checkpoint |
|---|-----------|-------------------|------------|------------|
| 2.1 | Создать `.gitignore` с правилами для macOS (`.DS_Store`), IDE-файлов (`.idea/`, `.vscode/`), и системных файлов | `.gitignore` | — | Файл создан, содержит стандартные исключения |
| 2.2 | Создать `README.md` с кратким описанием проекта: что это, структура файлов, как запустить локально (открыть `index.html` в браузере) | `README.md` | — | Файл создан, содержит название проекта, описание и инструкцию по запуску |

**Definition of done этапа:** В корне проекта есть `.gitignore` и `README.md`.

---

#### Этап 3: Инициализация git-репозитория и первый коммит

**Цель этапа:** Создать локальный git-репозиторий с осмысленным первым коммитом.

| # | Подзадача | Файлы/компоненты | Зависит от | Checkpoint |
|---|-----------|-------------------|------------|------------|
| 3.1 | Выполнить `git init` в директории `/Users/artemsidorov/it projects/intervo/` | корень проекта | 1.*, 2.* | `git status` показывает untracked-файлы |
| 3.2 | Убедиться, что ветка по умолчанию — `main` (если нет — переименовать через `git branch -M main`) | — | 3.1 | `git branch` показывает `main` |
| 3.3 | Добавить все файлы (`git add .`) и создать коммит с сообщением вида `feat: initial landing page` | все файлы проекта | 3.2 | `git log` показывает один коммит |

**Definition of done этапа:** Локальный git-репозиторий инициализирован, ветка `main`, один коммит со всеми файлами.

---

#### Этап 4: Создание репозитория на GitHub и push

**Цель этапа:** Опубликовать код в приватном репозитории `intervo` на аккаунте `art-ylem`.

| # | Подзадача | Файлы/компоненты | Зависит от | Checkpoint |
|---|-----------|-------------------|------------|------------|
| 4.1 | Проверить авторизацию в GitHub CLI: выполнить `gh auth status`. Если не авторизован — запросить у пользователя `gh auth login` | — | — | `gh auth status` показывает активную сессию для аккаунта `art-ylem` |
| 4.2 | Проверить, не существует ли репозиторий `intervo` в аккаунте: `gh repo view art-ylem/intervo` | — | 4.1 | Команда возвращает 404 (репозитория нет) или показывает существующий |
| 4.3 | Создать приватный репозиторий: `gh repo create intervo --private --source=. --remote=origin` | — | 4.2, 3.3 | Репозиторий создан, remote `origin` добавлен |
| 4.4 | Запушить код: `git push -u origin main` | — | 4.3 | Код доступен на `github.com/art-ylem/intervo` |

**Definition of done этапа:** Код опубликован в приватном репозитории `art-ylem/intervo` на GitHub.

---

#### Этап 5: Финальная верификация

**Цель этапа:** Убедиться, что всё работает и результат соответствует критериям готовности.

| # | Подзадача | Файлы/компоненты | Зависит от | Checkpoint |
|---|-----------|-------------------|------------|------------|
| 5.1 | Открыть `index.html` локально в браузере и проверить корректность отображения на desktop | `index.html` | 4.4 | Все секции отображаются, шрифты загружены, стили применены |
| 5.2 | Проверить мобильное отображение через DevTools (375px ширина) | `index.html` | 5.1 | Адаптивная вёрстка работает, нет горизонтального скролла |
| 5.3 | Убедиться, что репозиторий на GitHub содержит все файлы: `index.html`, `styles.css`, `.gitignore`, `README.md`, `docs/` | — | 4.4 | Все файлы присутствуют на GitHub |

**Definition of done этапа:** Лендинг корректно отображается локально, репозиторий содержит полный набор файлов.

---

### 🔗 ГРАФ ЗАВИСИМОСТЕЙ

```
1.1 ──┐
1.2 ──┤
1.3 ──┼──→ 3.1 → 3.2 → 3.3 ──→ 4.3 → 4.4 → 5.1 → 5.2
1.4 ──┤                    ↗                         ↓
1.5 ──┘                   /                         5.3
                         /
2.1 ────────────────────┘
2.2 ────────────────────┘

4.1 → 4.2 → 4.3 (параллельно с этапами 1-2)
```

**Параллельные работы:**
- Этап 1 (аудит) и Этап 2 (служебные файлы) можно выполнять параллельно
- Шаг 4.1 (проверка авторизации) можно выполнить в любой момент до 4.3

---

### ⚠️ РИСКИ ПЛАНА

| Риск | На каком шаге | Влияние | Митигация |
|------|---------------|---------|-----------|
| Нет авторизации `gh` CLI для аккаунта `art-ylem` | 4.1 | Высокое — push невозможен | Запросить у пользователя `gh auth login` перед шагом 4.3. Альтернатива — настроить SSH-ключ или использовать HTTPS с токеном |
| Репозиторий `intervo` уже существует | 4.2 | Среднее — конфликт при создании | Проверить через `gh repo view` перед созданием. Если существует — использовать как remote без пересоздания |
| Google Fonts CDN недоступен в момент проверки | 5.1 | Низкое — шрифты не отобразятся | Fallback-шрифты (`ui-sans-serif, system-ui, Georgia`) уже указаны в CSS — страница останется читаемой |
| В `index.html` или `styles.css` обнаружены проблемы при аудите | 1.1–1.5 | Среднее — потребуется доработка | Исправить до коммита (шаг 3.3). Объём доработки ограничен — файлы уже в хорошем состоянии |

---

### 🛡️ СТРАТЕГИЯ БЕЗОПАСНОСТИ

- Проект создаётся с нуля — нет существующего поведения, которое можно сломать
- До `git push` вся работа локальная и полностью обратимая
- Репозиторий создаётся как **private** — нет риска утечки до момента осознанного открытия
- Перед созданием репозитория проверяется его отсутствие — нет риска перезаписи существующего
- Файлы `docs/TASK_LANDING.md` и `docs/PLAN_LANDING.md` включаются в коммит — документация сохраняется вместе с кодом

---

### 📌 ДОПУЩЕНИЯ ПЛАНА

- Файлы `index.html` и `styles.css` уже содержат корректную структуру и стили — требуется аудит, а не переделка (подтверждено анализом кода)
- `gh` CLI установлен на машине пользователя
- Ветка по умолчанию — `main`
- Дополнительные файлы (favicon, robots.txt, Open Graph изображения) не входят в scope — достаточно `.gitignore` и `README.md`
- Деплой (GitHub Pages, Vercel) не входит в scope задачи

---

### ❓ ОТКРЫТЫЕ ВОПРОСЫ

- **Авторизация `gh` CLI:** токен может быть просрочен или привязан к другому аккаунту. Рабочая гипотеза: проверить на шаге 4.1, при необходимости запросить повторную авторизацию.
- **Нужен ли favicon?** Рабочая гипотеза: нет, не входит в scope. Можно добавить позже.
- **Включать ли папку `docs/` в репозиторий?** Рабочая гипотеза: да, документация полезна и не содержит конфиденциальных данных.

---

### ✅ DEFINITION OF DONE (ЗАДАЧА ЦЕЛИКОМ)

1. В папке `/Users/artemsidorov/it projects/intervo/` лежит чистый, валидный, адаптивный лендинг (`index.html` + `styles.css`)
2. Присутствуют служебные файлы: `.gitignore`, `README.md`
3. Git-репозиторий инициализирован, первый коммит создан на ветке `main`
4. Приватный репозиторий `intervo` создан на GitHub в аккаунте `art-ylem`
5. Код запушен в `origin/main`
6. Лендинг корректно отображается на desktop и mobile при открытии `index.html` в браузере

---

### 📦 ГОТОВО ДЛЯ ИСПОЛНИТЕЛЯ

**Порядок выполнения:** Этап 1 + Этап 2 (параллельно) → Этап 3 → Этап 4 → Этап 5

**Ограничения для Исполнителя:**
- Не добавлять JavaScript, сборщики или фреймворки — только статический HTML + CSS
- Не менять контент (тексты, цены, описания тарифов) — он финальный
- Не менять визуальный стиль — только исправлять баги вёрстки, если найдутся
- Не настраивать деплой (GitHub Pages, CI/CD) — только push кода
- Репозиторий должен быть **private**

**Первый шаг:** Проверить валидность `index.html` (шаг 1.1) и параллельно проверить авторизацию `gh auth status` (шаг 4.1), чтобы выявить блокеры на раннем этапе.
